[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "tablas-etl"
version = "0.4.0"
description = "Motor ETL multi-ERP y multi-base de datos (SQL Server, PostgreSQL, MySQL, SQLite)"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
dependencies = [
    "pandas>=2.0",
    "openpyxl",
    "sqlalchemy>=2.0",
    "pyodbc",
    "psycopg2-binary",
    "pymysql",
    "pyrfc",
    "python-dotenv",
    "requests",
    "fastapi",
    "uvicorn",
    "PyJWT",
    "bcrypt",
    "cryptography>=42",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "httpx",
]

[project.scripts]
etl = "cli:main"

[tool.setuptools]
packages = ["db", "db.sinks", "sources", "sources.sap", "sources.generic", "utils", "dashboard", "dashboard.auth", "derived", "bi", "licensing", "license_server"]
py-modules = ["cli", "etl_runner"]

[tool.ruff]
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
